# 安全说明

## 使用风险

m-ui 使用电视开放在局域网中的 ADB 和 HTTP 控制接口。这些接口通常没有适合不可信网络的访问控制，因此只应在可信家庭局域网中使用：

- 不要把电视的 TCP `5555`、`6095` 或路由器管理端口映射到公网。
- 不要在访客、公共或由不可信设备共享的网络中开启电视 ADB。
- 给路由器设置强密码，并限制 SSH 和管理页面只能从 LAN 访问。
- `/etc/m-ui/adbkey` 是路由器访问电视的私钥，应保持 `600` 权限，切勿提交或分享。
- 可选 Web 遥控器默认只监听 `127.0.0.1`；不要在没有额外身份验证的情况下将它监听到公网地址。

## 报告安全问题

请优先使用 GitHub 仓库的私密安全报告功能，不要在公开 Issue 中发布可直接利用的细节、家庭网络地址、密码或私钥。

报告时请说明受影响版本、环境、复现步骤和影响范围。任何日志或配置都应先删除 IP、MAC、设备序列号和密钥内容。

## 支持版本

项目规模较小，目前只为最新发布版本提供安全修复。电视厂商固件和未修改的第三方组件不在本项目的维护范围内。
