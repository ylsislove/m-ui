# 参与贡献

欢迎提交问题和改进。开始前请先搜索已有 Issue，避免重复报告。

提交代码时请：

1. 将改动聚焦在一个明确问题上。
2. 保持 `m-ui` 与 OpenWrt 常见 `/bin/sh` 兼容，不依赖 Bash 特性。
3. 为状态解析或行为变化补充测试。
4. 运行 `go test ./...` 和 `sh tests/test_m_ui.sh`。
5. 不要提交 ADB 私钥、SSH 密钥、真实家庭 IP、MAC、密码或路由器备份。

Bug 报告最好包含 m-ui 版本、路由器型号/架构、电视型号/系统版本、预期行为、实际行为和经过脱敏的日志。
